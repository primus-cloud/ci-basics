name: My first workflow

on: push

jobs:
    first_job:
            runs-on: ubuntu-latest
            steps:
            - name: Weclome message
              run: echo "my first github actions job"

            - name: List files
              run: ls

            - name: Read file
              run: cat README.md
