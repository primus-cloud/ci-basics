name: Demo Check

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Always Succeed  
        run: echo "check passed"

